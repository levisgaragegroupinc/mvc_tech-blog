<article>
    <div class="post-header-space">
        <div class="post-header">
            <h2>{{title}}</h2>
            <h2>{{user.username}}</h2>
        </div>    
        <div class="post-header">
            {{!-- <h2>{{format_date created_at}}</h2> --}}
            <h2>{{created_at}}</h2>
            <a href="/post/{{id}}">
                <h3>{{comment.length}}</h3>
                <h3>Comments</h3>
            </a>
        </div>
    </div>
</article>
